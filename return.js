function TenTimes(number) {
  const result = number * 10;
  return result;
}

function cutHalf(n) {
  const half = n / 2;
  return half;
}

const output = TenTimes(5);
const result = cutHalf(100);

console.log(output);
console.log(result);
